<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Constat Vie PDF</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 14px;
            margin: 0;
            padding: 0;
        }
        .container {
            width: 80%;
            margin: 20px auto;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 10px;
        }
        h1 {
            text-align: center;
            margin-bottom: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid #000;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        .signature {
            text-align: right;
            margin-top: 50px;
        }
    </style>
</head>
<body>
<div class="container">
    <h1>Informations sur le Constat de Vie</h1>
    <table>

        <tr>
            <th>Nom</th>
            <td>{{ constatvie.Nom ? constatvie.Nom }}</td>
        </tr>
        <tr>
            <th>Prenom</th>
            <td>{{ constatvie.Prenom ? constatvie.Prenom }}</td>
        </tr>

        <tr>
            <th>Carte d'identite </th>
            <td>{{ constatvie.CIN ? constatvie.CIN }}</td>
        </tr>
        <tr>
            <th>Date de Décès</th>
            <td>{{ constatvie.DateDeDeces ? constatvie.DateDeDeces|date('Y-m-d') : '' }}</td>
        </tr>
        <tr>
            <th>Cause de Décès</th>
            <td>{{ constatvie.CauseDeDeces }}</td>
        </tr>
        <tr>
            <th>Identifiant de l'Informant</th>
            <td>{{ constatvie.IdentifiantDeLinformant }}</td>
        </tr>
        {# Add more rows for additional fields if needed #}
    </table>
    <div class="signature">
        <p>Signature :</p>
    </div>
</div>
</body>
</html>
