<form method="post" action="{{ path('app_assurancevie_delete', {'id': assurancevie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ assurancevie.id) }}">
    <button class="btn">Delete</button>
</form>
