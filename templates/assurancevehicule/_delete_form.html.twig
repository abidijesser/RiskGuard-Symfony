<form method="post" action="{{ path('app_assurancevehicule_delete', {'id': assurancevehicule.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ assurancevehicule.id) }}">
    <button class="btn">Delete</button>
</form>
