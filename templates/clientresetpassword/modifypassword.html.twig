<!DOCTYPE html>
<html>
<head>

    <title>Modification de mot de passe</title>

    <link href="{{ asset('assets/css/modifypassword.css') }}" rel="stylesheet"/>
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link
            href="https://fonts.googleapis.com/css?family=Open+Sans:300,400,600,700" rel="stylesheet"/>
    <!-- Nucleo Icons -->
    <link href="{{ asset('assets/css/nucleo-icons.css') }}" rel="stylesheet"/>
    <link
            href="{{ asset('assets/css/nucleo-svg.css') }}" rel="stylesheet"/>
    <!-- Font Awesome Icons -->
    <script src="https://kit.fontawesome.com/42d5adcbca.js" crossorigin="anonymous"></script>
    <link
            href=" {{asset('assets/css/nucleo-svg.css')}}" rel="stylesheet"/>

    <script src="js/bootstrap.min.js"></script>

</head>
<body class="bg-gray">
<div class="form-container">
    <div class="logo-container">
        Modifier votre mot de passe
    </div>

    <form class="form" id="myform">
        <div class="form-group position-relative">
            <label for="password">Mot de passe</label>

            <div class="input-group">
                <input style="width: 100%" class="form-control" type="password" id="password" name="password" placeholder="Votre nouveau mot de passe">
                <div class="input-group-append">
                    <span class="input-group-text password-toggle-icon" onclick="togglePasswordVisibility()">
                        <i class="fas fa-eye-slash" ></i>
                    </span>
                </div>
            </div>


            <div id="passwordError" style="border: 1px solid red; border-radius: 3px; margin-top: 10px; padding-left: 4px; padding: 3px; display: none;">
                <div class="error-container">
                    <div class="icon-container">
                        <i class="fas fa-exclamation fa-xs" style="color: white;"></i>
                    </div>
                    <div class="error-message">Le champs <b>Mot de passe</b> est requis.</div>
                </div>
            </div>

            <div id="passwordRegex" style="border: 1px solid red; border-radius: 3px; margin-top: 10px; padding-left: 4px; padding: 3px; display: none;">
                <div class="error-container">
                    <div class="icon-container">
                        <i id="icon" class="fas fa-exclamation fa-xs" style="color: white;"></i>
                    </div>
                    <div class="error-message">Votre <b>mot de passe</b> doit contenir au moins <b>8</b> caractères.</div>
                </div>
            </div>
        </div>


        <div class="form-group" style="margin-top: 20px">
            <label for="confirmpassword" >Confirmer votre mot de passe</label>
            <input type="text" id="confirmpassword" name="email" placeholder="confirmer votre mot de passe" >

            <div id="confirmpasswordError" style="border: 1px solid red; border-radius:3px; margin-top:10px; padding-left:4px; padding:3px; display: none;">
                <div class="error-container">
                    <div class="icon-container">
                        <i class="fas fa-exclamation fa-xs" style="color: white;"></i>
                    </div>
                    <div class="error-message">Le champs <b>Confirmer votre mot de passe</b> est requis.</div>
                </div>
            </div>

            <div id="passwordmatchError" style="border: 1px solid red; border-radius:3px; margin-top:10px; padding-left:4px; padding:3px; display: none;">
                <div class="error-container">
                    <div class="icon-container">
                        <i id=" icon"class="fas fa-exclamation fa-xs" style="color: white;"></i>
                    </div>
                    <div class="error-message"><b>Mot de passe</b> ne correpond pas.</div>
                </div>
            </div>

        </div>

        <button class="form-submit-btn" type="submit">Envoyer code </button>

    </form>
</div>

<script>

    let myForm = document.getElementById('myform');
    let myRegex = /^.{8,}$/;

    let passwordInput = document.getElementById('password');
    let confirmpasswordInput = document.getElementById('confirmpassword');

    let passwordError = document.getElementById('passwordError');
    let passwordREGEX = document.getElementById('passwordRegex');
    let confirmpasswordError = document.getElementById('confirmpasswordError');
    let passwordmatchError = document.getElementById('passwordmatchError');

    myForm.addEventListener('submit', function(e) {
        if (passwordInput.value.trim() === "") {
            passwordError.style.display = 'block';
            passwordREGEX.style.display = 'none';
            passwordInput.style.border= '2px solid #ccc'
            e.preventDefault();

        } else if (!myRegex.test(passwordInput.value)) {
            passwordREGEX.style.display = 'block';
            passwordError.style.display = 'none';
            passwordInput.style.border= '2px solid #ccc'
            e.preventDefault();

        } else {
            passwordError.style.display = 'none'; // Hide both error messages if email is valid
            passwordREGEX.style.display = 'none';
            passwordInput.style.border= '2px solid #3A9B0E'
        }

        if (confirmpasswordInput.value.trim() === "") {
            confirmpasswordError.style.display = 'block';
            passwordmatchError.style.display = 'none';
            e.preventDefault();

        } else if (confirmpasswordInput.value.trim() !== passwordInput.value.trim() ) {
            passwordmatchError.style.display = 'block';
            confirmpasswordError.style.display = 'none';
            e.preventDefault();

        } else {
            passwordmatchError.style.display = 'none'; // Hide both error messages if email is valid
            confirmpasswordError.style.display = 'none';
        }
    });

    function toggleConfirmPasswordVisibility() {
        var confirmPasswordInput = document.getElementById('confirmpassword');
        var icon = document.querySelector('.password-toggle-iconn i ');

        if (confirmPasswordInput.type === "password") {
            confirmPasswordInput.type = "text";
            icon.classList.remove('fa-eye-slash');
            icon.classList.add('fa-eye');
        } else {
            confirmPasswordInput.type = "password";
            icon.classList.remove('fa-eye');
            icon.classList.add('fa-eye-slash');
        }
    }

    function togglePasswordVisibility() {
        var passwordInput = document.getElementById('password');
        var icon = document.querySelector('.password-toggle-icon i');

        if (passwordInput.type === "password") {
            passwordInput.type = "text";
            icon.classList.remove('fa-eye-slash');
            icon.classList.add('fa-eye');
        } else {
            passwordInput.type = "password";
            icon.classList.remove('fa-eye');
            icon.classList.add('fa-eye-slash');
        }
    }

</script>

</body>
</html>
