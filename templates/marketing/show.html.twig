{% extends 'base.html.twig' %}

{% block title %}Marketing{% endblock %}

{% block body %}
    <h1>Marketing</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marketing.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ marketing.titre }}</td>
            </tr>
            <tr>
                <th>Objectif</th>
                <td>{{ marketing.objectif }}</td>
            </tr>
            <tr>
                <th>Budget</th>
                <td>{{ marketing.budget }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ marketing.dateDebut ? marketing.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ marketing.dateFin ? marketing.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ marketing.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_marketing_index') }}">back to list</a>

    <a href="{{ path('app_marketing_edit', {'id': marketing.id}) }}">edit</a>

    {{ include('marketing/_delete_form.html.twig') }}
{% endblock %}
